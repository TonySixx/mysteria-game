import React from 'react';
import { useThree } from '@react-three/fiber';

function GameScene() {
  const { scene } = useThree();

  // Zde budeme přidávat herní objekty a logiku

  return null;
}

export default GameScene;